---
description: Правила для работы с проектом Gold Apple Frontend
globs:
alwaysApply: true
---

# Правила для работы с проектом Gold Apple Frontend

## Основные принципы

- Отвечай на русском языке
- Следуй принципам Domain-Driven Design (DDD) и модульной архитектуры
- Используй Atomic Design для организации UI-компонентов
- Оптимизируй производительность и Web Vitals
- Следуй официальной документации Nuxt.js и Vue.js

### 🔧 wiki/workflow/ - Процессы разработки

# Архитектура

[TOC]

## Введение

Данный документ представляет собой техническую документацию проекта. Проект разработан с акцентом на масштабируемость, поддерживаемость и гибкость, что достигается за счет внедрения принципов Domain-Driven Design (DDD), элементов Чистой архитектуры и Atomic Design для организации UI-компонентов.

Цель документа
Описание ключевых архитектурных решений, процессов и компонентов системы, включая:

- Организацию бизнес-логики через ограниченные контексты и агрегаты.
- Взаимодействие модулей через сервисы, репозитории и шину событий.
- Особенности реализации маршрутизации, рендеринга (SSR/SPA/WebView) и управления состоянием.
- Интеграцию с внешними API, аналитикой и системами авторизации.

## Архитектурные принципы и решения

### Domain-Driven Design (DDD) в проекте

Проект построен на принципах Domain-Driven Design, что позволяет эффективно организовать сложную бизнес-логику интернет-**магазина** вокруг бизнес-доменов:

1. **Ограниченные контексты**:

   - Каждый модуль представляет собой отдельный ограниченный контекст с четко определенными границами и ответственностью
   - Модули `auth`, `cart`, `checkout`, `pdp`, `plp` и другие (всего более 40) соответствуют конкретным бизнес-доменам ([подробнее о модулях](/wiki/workflow/modules.md))
   - Каждый контекст имеет свой собственный набор хранилищ, сервисов и репозиториев, что предотвращает смешивание концепций

2. **Многослойная архитектура внутри доменов**:

   - **Слой представления**: компоненты UI, отвечающие за отображение данных
   - **Слой приложения**: сервисы, координирующие бизнес-логику и взаимодействие между компонентами
   - **Слой домена**: бизнес-правила и логика, инкапсулированные в сервисах
   - **Слой инфраструктуры**: репозитории, обеспечивающие доступ к внешним API и данным
   - **Слой состояния**: сторы, управляющие состоянием доменных объектов

3. **Единый язык**:

   - Код модулей использует термины из бизнес-лексики (product, cart, checkout, auth)
   - Структура данных и методы отражают модели предметной области и бизнес-процессы
   - Согласованная терминология между кодом, документацией и общением команды

4. **Сервисы домена и приложения**:
   - Сервисы домена инкапсулируют бизнес-логику, которая не вписывается в отдельные сущности
   - Сервисы приложения координируют взаимодействие между различными доменами
   - Четкое разделение ответственности между сервисами

### Многослойная архитектура

Проект следует принципам Чистой архитектуры с четким разделением на функциональные слои:

1. **Модули как бизнес-домены**:

   - Проект организован вокруг бизнес-доменов (модулей), а не функциональных частей интерфейса
   - Каждый модуль (auth, cart, checkout, pdp) представляет собой отдельный бизнес-домен с полным набором слоев
   - Поток данных в модулях:
     - **Нисходящий поток**: компонент работает с данным полученными из сторов. Данные могут быть переданы как через пропсы, так и запрошены из стора по месту
     - **Восходящий поток**: компоненты вызывают методы сервисов или компосаблов, которые обновляют сторы
   - Ограничения взаимодействия:
     - Компоненты не должны напрямую изменять сторы или совершать работу над данными. Такого рода работа должна выполняться только через сервисы
     - Репозитории отвечают только за получение сырых данных без бизнес-логики

2. **Слои (Layers)**:

   - Архитектура следует принципу вертикального разделения на слои:
     - `components` - UI-компоненты разных уровней абстракции, содержащие только UI-логику
     - `services` - бизнес-логика и координация между компонентами
     - `repositories` - взаимодействие с API и получение сырых данных
     - `stores` - управление состоянием данных
     - `analytics` - отслеживание событий и метрик
   - Каждый слой имеет четкую ответственность и следует принципу инверсии зависимостей, где все компоненты зависят от центрального объекта (`$gaApp`)

3. **Atomic Design для UI-компонентов**:
   - Внутри слоев компонентов используется методология Atomic Design с разделением по уровням абстракции:
     - `atoms` - базовые компоненты (кнопки, поля ввода)
     - `molecules` - комбинации атомов (формы, карточки)
     - `organisms` - сложные составные компоненты
     - `templates` - шаблоны страниц
   - Такая структура обеспечивает переиспользование компонентов и упрощает поддержку

## Процесс запуска приложения

Приложение построено на фреймворке Nuxt.js и использует два основных режима рендеринга: SSR (Server-Side Rendering) и SPA (Single Page Application). Важной особенностью является то, что эти режимы используются взаимоисключающе - либо SSR, либо SPA, без промежуточного этапа гидратации.

### Точки входа и конфигурация

1. **Основная конфигурация**:

   - Файл `nuxt.config.js` определяет базовые параметры приложения
   - Режим SSR включается через настройку: `ssr: config.get('ssr')`

2. **Плагины инициализации**:

   - `init.server.js` - точка входа для серверного рендеринга (SSR)
   - `init.client.js` - точка входа для клиентского рендеринга (SPA)
   - Эти плагины запускают соответствующие классы инициализации

3. **Серверные middleware**:
   - Обрабатывают запросы перед рендерингом страницы
   - Позволяют динамически переключаться между режимами SSR и SPA
   - Определяют тип клиента (бот или обычный пользователь)

### Режимы рендеринга

1. **SSR (Server-Side Rendering)**:

   - Используется преимущественно для поисковых ботов
   - Полная отрисовка HTML на сервере
   - Оптимизирован для SEO и первого отображения контента

2. **SPA (Single Page Application)**:

   - Используется для обычных пользователей
   - Полностью клиентский рендеринг после загрузки JavaScript
   - Обеспечивает более интерактивный пользовательский опыт

3. **WebView режим**:
   - Специальный режим для встраивания в мобильные приложения
   - Упрощенная инициализация с отключением некоторых компонентов

### Процесс запуска на сервере (SSR)

При запросе страницы на сервере выполняется следующая последовательность:

1. **Запрос → Nuxt SSR → init.server.js → NuxtServer.init()**

2. **Основные этапы инициализации**:

   - Установка флага серверного рендеринга (`setSSR`)
   - Настройка перехватчиков API-запросов
   - Загрузка начальных данных (навигация, общие данные, feature toggles)
   - Проверка критических ошибок
   - Установка локализации
   - Определение типа клиента (бот/пользователь)
   - Установка локации по умолчанию

3. **Особенности серверного рендеринга**:
   - Оптимизация для поисковых ботов
   - Предварительная загрузка критических данных
   - Возможность переключения в SPA-режим при ошибках или таймаутах

### Процесс запуска в браузере (SPA)

При запуске приложения в браузере выполняется следующая последовательность:

1. **Запрос → Nuxt SPA → init.client.js → Выбор режима инициализации**

2. **Выбор режима инициализации**:

   ```
   NuxtClient - стандартный режим для обычных пользователей
   NuxtWebview - режим для встраивания в мобильные приложения
   ```

3. **Основные этапы инициализации в стандартном режиме**:

   - Настройка перехватчиков API
   - Инициализация идентификатора устройства
   - Подготовка клиентских данных
   - Настройка слушателей событий окна и пользовательского ввода
   - Определение текущего брейкпоинта и поддержки hover
   - Установка локализации и идентификатора сеанса
   - Инициализация авторизации и определение местоположения пользователя
   - Настройка разрешений для cookie и защиты маршрутов
   - Инициализация модальных окон и аналитики

4. **Особенности WebView режима**:
   - Упрощенная инициализация
   - Отсутствие некоторых компонентов (авторизация, cookie permissions)
   - Специальные настройки для взаимодействия с нативным приложением

### Стратегия переключения режимов

1. **Определение режима**:

   - По умолчанию используется режим, указанный в конфигурации
   - Серверные middleware могут динамически переключать режим

2. **Механизмы переключения**:

   - Заголовок `x-turn-on-spa-mode` для принудительного SPA-режима
   - Заголовок `x-critical-error` для переключения в SPA при ошибках
   - Определение бота через заголовок `x-customer-is-bot`

3. **Преимущества подхода**:
   - Гибкость в выборе оптимального режима для разных сценариев
   - Возможность деградации до SPA при проблемах с SSR
   - Оптимизация для разных типов клиентов

## Маршрутизация

Процесс обработки маршрута происходит следующим образом:

1. **Запрос маршрута**:
   - Все запросы обрабатываются файлом `client/pages/_.vue`
   - В хуке `asyncData` вызывается метод `context.$services.app.router.getData(context)`
2. **Определение компонента страницы**:

   - Сервис роутера получает данные о маршруте из API-сервиса редиректов
   - Для получения данных о маршруте используется метод `getRedirectData` из `modules/app/repositories/main.repository.js`
   - API-эндпоинт `catalog/redirect` возвращает информацию о типе модуля (`moduleType`) и типе страницы (`pageType`) для текущего URL
   - На основе полученных данных находится соответствующий маршрут в массиве зарегистрированных маршрутов
   - Определяет компонент страницы на основе `moduleType` и `pageType`. [Подробнее о роутах модуля](/wiki/workflow/modules.md#routes).

3. **Получение данных для страницы**:

   - Для каждого модуля может быть определен метод `getData` в сервисе `page` [Подробнее про getData](/wiki/features/routing.md#pageservice)
   - Этот метод вызывается для предварительной обработки данных страницы
   - Результаты обработки передаются в компонент страницы

4. **Рендеринг страницы**:

   - Компонент `_.vue` рендерит динамический компонент с помощью тега `<component :is="pageComponent">`
   - Передает данные страницы через проп `redirect`

5. **Обработка редиректов**:
   - Если сервис редиректов возвращает поле `redirect` в ответе, происходит автоматическое перенаправление на указанный URL
   - Это позволяет серверу управлять маршрутизацией и перенаправлять пользователей на актуальные URL
   - Механизм используется для обработки устаревших URL, канонизации URL и реализации бизнес-логики маршрутизации

## Взаимодействие модулей

Модули в архитектуре приложения могут взаимодействовать между собой следующими способами:

1. **Через сервисы**:

   - Модули получают доступ к сервисам друг друга через центральный объект `gaApp`
   - Это позволяет вызывать методы и использовать функциональность других модулей через четко определенные интерфейсы

     **Пример**: Модуль `header` предоставляет сервис `setHeaderRegularColorScheme`, который используется в других модулях:

   ```javascript
   // В сервисе модуля header
   class ThemeService {
     // ...
     setHeaderRegularColorScheme() {
       this.state.headerColorScheme = COLOR_SCHEME.REGULAR;
     }
     // ...
   }
   ```

   **Использование в компоненте модуля brandzone**:

   ```javascript
   $gaApp.services.header.theme.setHeaderRegularColorScheme();
   ```

2. **Через composables**:

   - Composables предоставляют удобный способ доступа к функциональности и состоянию модулей в формате Vue Composition API
   - Модули экспортируют composables, которые могут быть использованы в компонентах других модулей

   **Пример**: Модуль `location` экспортирует `useAddressChange`, который используется во многих других модулях:

   ```javascript
   // В модуле location/use/address-change.js
   import { useContext } from "@nuxtjs/composition-api";

   import { onUnmounted } from "vue";

   export const useAddressChange = (addressChangeCallback) => {
     const { $gaApp } = useContext();

     const unsubscribe = $gaApp.eventBus.subscribe(
       "module/location/address-change",
       addressChangeCallback
     );

     onUnmounted(() => {
       unsubscribe();
     });
   };
   ```

   **Использование в модуле `plp`**:

   ```javascript
   import { useAddressChange } from "~/modules/location";

   // В setup компонента
   useAddressChange(() => {
     doScrollToFiltersPanel();

     $gaApp.services.plp.listing.onAddressChange();
     $gaApp.services.plp.media.startTimerAutoplay();
   });
   ```

   **Использование в модуле `pdp`**:

   ```javascript
   import { useAddressChange } from "~/modules/location";

   // В setup компонента
   useAddressChange(() => {
     // Обновление данных при изменении адреса
     fetchData();
   });
   ```

3. **Через шину событий (Event Bus)**:

   - Модули могут взаимодействовать через шину событий `gaApp.eventBus`, используя паттерн публикация/подписка (publish/subscribe)
   - Это позволяет модулям слабо связываться между собой, так как они не зависят напрямую друг от друга

   **Пример публикации события**: Модуль `location` публикует событие при изменении адреса:

   ```javascript
   // В сервисе модуля location
   gaApp.eventBus.publish("module/location/address-change");
   ```

   **Пример подписки на событие**: Другие модули могут подписаться на это событие:

   ```javascript
   // В модуле location/use/address-change.js
   const unsubscribe = $gaApp.eventBus.subscribe(
     "module/location/address-change",
     addressChangeCallback
   );
   ```

   **Пример использования в модуле `modal`**:

   ```javascript
   // Публикация события открытия модального окна
   this.gaApp.eventBus.publish("module/modal/open", { name, data, push, type });

   // Подписка на событие открытия модального окна
   const unsubscribe = this.gaApp.eventBus.subscribe(
     "module/modal/open",
     (data) => {
       // Обработка события открытия модального окна
     }
   );
   ```

Таким образом, модули могут взаимодействовать тремя способами: через сервисы, доступные в объекте `gaApp`, через composables, которые предоставляют удобный доступ к функциональности в формате Vue Composition API, и через шину событий, которая позволяет реализовать слабосвязанную архитектуру.

## Регистрация модулей

Процесс регистрации модулей в приложении происходит через метод `installModules` класса `GaApp`, который выполняет следующие действия:

1. **Импорт модулей**:

   - Все модули импортируются в файле `client/plugins/modules.js`
   - Каждый модуль экспортируется из своей директории через индексный файл

   ```javascript
   // В файле client/plugins/modules.js
   import { location } from "~/modules/location";
   import { modal } from "~/modules/modal";
   import { pdp } from "~/modules/pdp";
   // ... и другие модули

   export default (context, inject) => {
     const gaApp = context.$gaApp;

     gaApp.installModules([
       location,
       modal,
       pdp,
       // ... и другие модули
     ]);

     inject("stores", gaApp.modules.stores);
     inject("repositories", gaApp.modules.repositories);
     inject("services", gaApp.modules.services);
   };
   ```

2. **Структура модуля**:

   - Каждый модуль представляет собой объект с метаданными, компонентами и слоями
   - Слои включают сервисы, хранилища, репозитории и аналитику

   ```javascript
   // В файле client/modules/location/index.js
   export const location = {
     meta: {
       name: "location", // Уникальное имя модуля
       locales, // Локализации
     },
     components, // Vue компоненты
     layers: {
       stores, // Хранилища состояния
       services, // Сервисы
       repositories, // Репозитории для работы с данными
       analytics, // Аналитика
     },
   };
   ```

3. **Процесс установки модулей**:

   - Метод `installModules` в классе `GaApp` обрабатывает каждый модуль
   - Проверяет наличие имени модуля в его метаданных
   - Регистрирует маршруты, если они определены в модуле
   - Добавляет локализации в общий список локалей приложения
   - Регистрирует компоненты в глобальном пространстве имен Vue
   - Интегрирует слои (сервисы, хранилища, репозитории) в общую систему приложения

   ```javascript
   // В файле client/modules/app/core/app.js
   class GaApp {
     // ...
     installModules(modules) {
       modules.forEach((module) => {
         const { meta = {}, components = {}, layers = {} } = module;

         if (!meta.name) {
           return;
         }

         // Регистрация маршрутов
         if (meta.routes) {
           meta.routes.forEach((route) => {
             this.routes.push(route);
           });
         }

         // Регистрация локализаций
         if (meta.locales?.[this.config.get("locale.default")]) {
           // ... код для регистрации локализаций
         }

         // Регистрация компонентов
         Object.keys(components).forEach((name) => {
           Vue.component(name, components[name]);
         });

         // Интеграция слоев
         Object.keys(layers).forEach((layer) => {
           if (layer === "analytics") {
             this.analytics.modules[meta.name] = layers[layer](
               this,
               this.analytics.libs
             );
           } else {
             this.modules[layer][meta.name] = layers[layer](this);
           }
         });
       });
     }
     // ...
   }
   ```

4. **Доступ к модулям**:
   - После регистрации модули доступны через объект `gaApp`
   - Сервисы: `gaApp.services.moduleName.serviceName`
   - Хранилища: `gaApp.stores.moduleName.storeName`
   - Репозитории: `gaApp.repositories.moduleName.repositoryName`
